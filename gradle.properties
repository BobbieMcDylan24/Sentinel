# Project settings
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true

# Plugin version
version=1.0.0

# Test server path (optional - set this to auto-copy to your test server)
# testServerPath=C:/MinecraftServers/TestServer
